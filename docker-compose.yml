version: "3.9"

services:
  eieimarketplace-vendor-reservation:
    container_name: eiei-reservation
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "7003:7003"
